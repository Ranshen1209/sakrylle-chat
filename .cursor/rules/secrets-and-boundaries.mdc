---
description: Secrets、fallback 机制与变更边界约束
alwaysApply: true
---

# Secrets and Fallback Mechanisms

- **禁止**在源码中提交真实 secrets。
- `lib/secrets/fallback.dart` 当前为占位实现；CI 在多个 workflow 中注入真实值。**禁止**将真实 key 写入仓库。
- 禁止为「让它跑起来」静默添加 fallback key、fallback API 或吞错逻辑。
- 若确实需要 fallback，须**全部**满足：
  - 显式开关
  - 清晰日志
  - 可禁用
  - 任务描述中记录原因

# Change Boundary and Duplicate Workflows

- 本仓库有多个相似的 GitHub Actions workflow（尤其构建类）。触碰 build、版本号或注入逻辑时，须检查**所有**相似 workflow 并同步。
- 勿因发现「可以统一」而扩大 scope；先完成当前任务，再决定是否开独立重构任务。
- 触碰 path 依赖时，将其视为独立模块；勿只在根仓库表面打补丁。
