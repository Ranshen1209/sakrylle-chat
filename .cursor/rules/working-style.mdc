---
description: 工作风格、执行顺序与设计原则
alwaysApply: true
---

# Working Style

- **全程用中文沟通**。聚焦当前任务，不做空泛建议。
- **事实优先**。结论必须基于当前代码、配置、测试、构建脚本或 git 状态，禁止猜测。
- **Debug-first**。禁止静默降级、吞错、隐藏 fallback 或假成功分支来「让它跑起来」。
- **KISS / YAGNI**：最直接、可验证的方案优先；不为「架构完整性」预埋多余抽象或配置开关。
- **SOLID 是工具而非目标**：仅在真正降低耦合、提升可读性时拆分；勿为形式分层打碎简单逻辑。
- **最小闭环**：只做当前任务所需的最小改动，不顺手修无关问题。
- **并行收集上下文**：独立的文件读取、`rg` 搜索、`git status`、配置检查应批量并行，勿串行化。
- 复杂任务动手前写简要 Mini Control Contract：
  - `Primary Setpoint`：必须达成什么
  - `Acceptance`：什么命令/测试/行为可证明
  - `Guardrails`：副作用中什么不能破
  - `Boundary`：涉及哪些文件/模块
  - `Risks`：1–3 个关键风险

# Recommended Execution Order

1. `git status --short` — 确认工作区基线。
2. 阅读相关代码与配置，写清验收标准。Desktop 任务先确认入口拓扑：`main.dart` → `lib/desktop/**` → 共享聊天布局。
3. 并行批量读取/搜索/状态检查，再决定最小改动落点。
4. 先列需求场景与验证方式，再做最小改动；不混入无关重构。
5. 运行与本任务相关的生成、格式化、分析、测试命令。
6. 自审 `git diff`：本地化、生成文件、兼容性风险、无关改动。
7. 交付时明确说明：改了什么、跑了什么命令、跳过了什么验证、残留风险。

# Design Principles

- 可读性优先。代码给人读，不是炫技。
- 反对臃肿实现、闲置抽象、学术过度设计。
- 能减复杂度就减，能少分支就少，能少一层间接就少。
- 简单、稳定、可验证优先；「优雅」其次。
- 避免双重状态与双重真相，保持单一真相源。
- 只写当下需要的，但要写对。
- 错误信息要有用，能定位与恢复，而非只说「失败」。
- 机制优于魔法常量；硬编码阈值须说明原因与边界。
- 能小步验证时，不做大的不可逆改动。

# Historical Pitfall Log

> 在此记录开发中遇到的、对未来有复用价值的重大陷阱。

- 只记录本仓库实际发生的问题。
- 不写「听说可能会」的条目。
- 新增条目优先用「症状 → 根因 → 修复/约束」格式。
