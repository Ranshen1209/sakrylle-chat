name: Deprecated Stable 3.38 Build

on:
  workflow_dispatch:

jobs:
  deprecated:
    name: Deprecated workflow
    runs-on: ubuntu-latest
    steps:
      - name: Use the stable build workflow
        run: |
          echo "This Flutter 3.38 workflow is deprecated. Use '.github/workflows/build-stable.yml' for current Sakrylle Chat builds."
          exit 1
