name: Flutter Multi-Platform Build Stable

env:
  FLUTTER_VERSION: '3.44.1'
  APP_DISPLAY_NAME: 'Sakrylle Chat'
  APP_ARTIFACT_NAME: 'Sakrylle_Chat'
  LINUX_PACKAGE_NAME: 'sakrylle-chat'
  LINUX_APP_ID: 'com.sakrylle.chat'
  LINUX_EXECUTABLE: 'sakrylle_chat'
  WINDOWS_EXECUTABLE: 'sakrylle_chat.exe'

on:
  # 手动触发
  workflow_dispatch:
    inputs:
      # 选择构建的平台
      build_android:
        description: '构建 Android'
        required: false
        default: true
        type: boolean

      build_ios:
        description: '构建 iOS'
        required: false
        default: true
        type: boolean

      build_mac:
        description: '构建 macOS'
        required: false
        default: true
        type: boolean

      build_windows:
        description: '构建 Windows'
        required: false
        default: true
        type: boolean

      build_linux:
        description: '构建 Linux'
        required: false
        default: true
        type: boolean

      # 是否发布到 Release（默认不发布）
      publish_release:
        description: '发布到 Release'
        required: false
        default: false
        type: boolean

      # Release 标签（只有选择发布时才需要填写）
      release_tag:
        description: 'Release 标签（如 v1.0.0）'
        required: false
        default: ''
        type: string

      # 是否为预发布版本
      prerelease:
        description: '是否为预发布版本 (Pre-release)'
        required: false
        default: false
        type: boolean

jobs:
  # Android 构建
  android:
    if: ${{ github.event.inputs.build_android == 'true' }}
    name: 构建 Android
    runs-on: ubuntu-latest
    permissions: write-all

    steps:
      - name: 📦 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ☕ 配置 Java 环境
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'

      - name: 🐦 安装 Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: 📝 Inject fallback
        shell: bash
        run: |
          mkdir -p lib/secrets
          cat > lib/secrets/fallback.dart <<'EOF'
          const String siliconflowFallbackKey = '${{ secrets.SILICONFLOW_KEY }}';
          EOF
      - name: 📝 获取版本号
        id: version
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | sed 's/version: //' | tr -d ' ')
          if [ "${{ github.event.inputs.publish_release }}" != "true" ]; then
            SHORT_SHA=$(git rev-parse --short HEAD)
            VERSION="${VERSION}_${SHORT_SHA}"
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "版本号: $VERSION"
      # 如果有签名密钥，写入密钥文件
      - name: 🔑 写入签名密钥
        env:
          SIGN_KEYSTORE_BASE64: ${{ secrets.SIGN_KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          if [ -z "$SIGN_KEYSTORE_BASE64" ]; then
            echo "未配置 Android 签名密钥，跳过签名配置"
            exit 0
          fi
          if [ -z "$KEYSTORE_PASSWORD" ] || [ -z "$KEY_ALIAS" ] || [ -z "$KEY_PASSWORD" ]; then
            echo "SIGN_KEYSTORE_BASE64 已配置，但 KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD 不完整"
            exit 1
          fi
          echo "$SIGN_KEYSTORE_BASE64" | base64 --decode > android/app/key.jks
          echo "storeFile=key.jks" >> android/key.properties
          echo "storePassword=$KEYSTORE_PASSWORD" >> android/key.properties
          echo "keyAlias=$KEY_ALIAS" >> android/key.properties
          echo "keyPassword=$KEY_PASSWORD" >> android/key.properties
          echo "✅ 签名配置已写入"
      - name: 🏗️ 构建 APK
        run: flutter build apk --release --split-per-abi

      - name: 📦 重命名 APK
        run: |
          cd build/app/outputs/flutter-apk
          for file in app-*-release.apk; do
            abi=$(echo "$file" | sed -E 's/app-(.*)-release\.apk/\1/')
            mv "$file" "${APP_ARTIFACT_NAME}_android_${VERSION}_${abi}.apk"
          done
      - name: 🚀 发布到 Release
        if: ${{ github.event.inputs.publish_release == 'true' && github.event.inputs.release_tag != '' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.release_tag }}
          name: ${{ github.event.inputs.release_tag }}
          prerelease: ${{ github.event.inputs.prerelease }}
          files: build/app/outputs/flutter-apk/Sakrylle_Chat_android_*.apk

      - name: 📤 上传构建产物 (arm64-v8a)
        uses: actions/upload-artifact@v4
        with:
          name: Android-arm64-v8a
          path: build/app/outputs/flutter-apk/Sakrylle_Chat_android_*_arm64-v8a.apk

      - name: 📤 上传构建产物 (armeabi-v7a)
        uses: actions/upload-artifact@v4
        with:
          name: Android-armeabi-v7a
          path: build/app/outputs/flutter-apk/Sakrylle_Chat_android_*_armeabi-v7a.apk

      - name: 📤 上传构建产物 (x86_64)
        uses: actions/upload-artifact@v4
        with:
          name: Android-x86_64
          path: build/app/outputs/flutter-apk/Sakrylle_Chat_android_*_x86_64.apk

  # iOS 构建
  ios:
    if: ${{ github.event.inputs.build_ios == 'true' }}
    name: 构建 iOS
    runs-on: macos-latest
    permissions: write-all

    steps:
      - name: 📦 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐦 安装 Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: 📝 Inject fallback
        shell: bash
        run: |
          mkdir -p lib/secrets
          cat > lib/secrets/fallback.dart <<'EOF'
          const String siliconflowFallbackKey = '${{ secrets.SILICONFLOW_KEY }}';
          EOF
      - name: 📝 获取版本号
        id: version
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | sed 's/version: //' | tr -d ' ')
          if [ "${{ github.event.inputs.publish_release }}" != "true" ]; then
            SHORT_SHA=$(git rev-parse --short HEAD)
            VERSION="${VERSION}_${SHORT_SHA}"
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "版本号: $VERSION"
      - name: 🏗️ 构建 iOS (无签名)
        run: |
          flutter build ios --release --no-codesign
          cd build/ios/iphoneos
          mkdir Payload
          cp -r Runner.app Payload/
          zip -r ../../../${APP_ARTIFACT_NAME}_ios_${VERSION}.ipa Payload
      - name: 🚀 发布到 Release
        if: ${{ github.event.inputs.publish_release == 'true' && github.event.inputs.release_tag != '' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.release_tag }}
          name: ${{ github.event.inputs.release_tag }}
          prerelease: ${{ github.event.inputs.prerelease }}
          files: Sakrylle_Chat_ios_*.ipa

      - name: 📤 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: iOS-IPA
          path: Sakrylle_Chat_ios_*.ipa

  # macOS 构建
  macos:
    if: ${{ github.event.inputs.build_mac == 'true' }}
    name: 构建 macOS
    runs-on: macos-latest
    permissions: write-all

    steps:
      - name: 📦 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐦 安装 Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: 📝 Inject fallback
        shell: bash
        run: |
          mkdir -p lib/secrets
          cat > lib/secrets/fallback.dart <<'EOF'
          const String siliconflowFallbackKey = '${{ secrets.SILICONFLOW_KEY }}';
          EOF
      - name: 📝 获取版本号
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | sed 's/version: //' | tr -d ' ')
          if [ "${{ github.event.inputs.publish_release }}" != "true" ]; then
            SHORT_SHA=$(git rev-parse --short HEAD)
            VERSION="${VERSION}_${SHORT_SHA}"
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV
      - name: 🏗️ 构建 macOS
        run: flutter build macos --release

      - name: 📦 创建 DMG
        run: |
          hdiutil create -volname "$APP_DISPLAY_NAME" -srcfolder "build/macos/Build/Products/Release/$APP_DISPLAY_NAME.app" -ov -format UDZO "${APP_ARTIFACT_NAME}_macos_${VERSION}.dmg"
      - name: 🚀 发布到 Release
        if: ${{ github.event.inputs.publish_release == 'true' && github.event.inputs.release_tag != '' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.release_tag }}
          name: ${{ github.event.inputs.release_tag }}
          prerelease: ${{ github.event.inputs.prerelease }}
          files: Sakrylle_Chat_macos_*.dmg

      - name: 📤 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: macOS-DMG
          path: Sakrylle_Chat_macos_*.dmg

  # Windows 构建
  windows:
    if: ${{ github.event.inputs.build_windows == 'true' }}
    name: 构建 Windows
    runs-on: windows-latest
    permissions: write-all

    steps:
      - name: 📦 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔧 安装最新 CMake
        uses: lukka/get-cmake@latest

      - name: 🐦 安装 Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: 📝 Inject fallback
        shell: bash
        run: |
          mkdir -p lib/secrets
          cat > lib/secrets/fallback.dart <<'EOF'
          const String siliconflowFallbackKey = '${{ secrets.SILICONFLOW_KEY }}';
          EOF
      - name: 🔧 获取依赖
        run: flutter pub get

      - name: 🔍 验证 CMake 版本
        shell: pwsh
        run: |
          Write-Host "检查 CMake 版本..."
          cmake --version
          $cmakeVersion = (cmake --version | Select-String "cmake version" | ForEach-Object { $_.ToString() })
          Write-Host "当前 CMake: $cmakeVersion"
      - name: 📝 获取版本号
        shell: pwsh
        run: |
          $version = (Select-String -Path pubspec.yaml -Pattern "version:" | ForEach-Object { $_.Line.Split(":")[1].Trim() })
          if ("${{ github.event.inputs.publish_release }}" -ne "true") {
            $shortSha = git rev-parse --short HEAD
            $version = "$version`_$shortSha"
          }
          echo "VERSION=$version" >> $env:GITHUB_ENV
          Write-Host "版本号: $version"
      - name: 🏗️ 构建 Windows
        run: flutter build windows --release

      - name: 📦 打包 ZIP
        shell: pwsh
        run: |
          Compress-Archive -Path "build/windows/x64/runner/Release/*" -DestinationPath "$env:APP_ARTIFACT_NAME`_windows_$env:VERSION.zip"
      - name: 🔧 安装 Inno Setup
        run: choco install innosetup -y

      - name: 📦 创建安装程序
        shell: pwsh
        run: |
          # 语言文件检测（中文语言包在部分 Runner 缺失，避免编译失败）
          $zhLangCompiler = "C:\\Program Files (x86)\\Inno Setup 6\\Languages\\ChineseSimplified.isl"
          $zhLangLocalDir = Join-Path (Get-Location) "Languages"
          $zhLangLocal = Join-Path $zhLangLocalDir "ChineseSimplified.isl"
          $languagesBlock = $null
          if (Test-Path $zhLangCompiler) {
            Write-Host "✅ 检测到中文语言文件: $zhLangCompiler"
            $languagesBlock = @"
          [Languages]
          Name: "chinesesimplified"; MessagesFile: "compiler:Languages\ChineseSimplified.isl"
          Name: "english"; MessagesFile: "compiler:Default.isl"
          "@
          } else {
            Write-Host "⚠️ 未在 Inno Setup 安装目录找到中文语言文件，尝试下载到本地项目..."
            try {
              New-Item -ItemType Directory -Force -Path $zhLangLocalDir | Out-Null
              $uri = 'https://raw.githubusercontent.com/jrsoftware/issrc/main/Files/Languages/ChineseSimplified.isl'
              Invoke-WebRequest -Uri $uri -OutFile $zhLangLocal -UseBasicParsing -TimeoutSec 60
              if (Test-Path $zhLangLocal) {
                Write-Host "✅ 已下载中文语言文件: $zhLangLocal"
                $languagesBlock = @"
          [Languages]
          Name: "chinesesimplified"; MessagesFile: "$zhLangLocal"
          Name: "english"; MessagesFile: "compiler:Default.isl"
          "@
              }
            } catch {
              Write-Host "⚠️ 下载中文语言文件失败: $($_.Exception.Message)"
            }
            if (-not $languagesBlock) {
              Write-Host "ℹ️ 将仅包含英文安装向导"
              $languagesBlock = @"
          [Languages]
          Name: "english"; MessagesFile: "compiler:Default.isl"
          "@
            }
          }
          # 创建 Inno Setup 脚本
          $issContent = @"
          #define MyAppName "Sakrylle Chat"
          #define MyAppVersion "$env:VERSION"
          #define MyAppPublisher "Sakrylle"
          #define MyAppExeName "$env:WINDOWS_EXECUTABLE"
          [Setup]
          AppId={{A7B8C9D0-E1F2-4A5B-8C9D-0E1F2A3B4C5D}}
          AppName={#MyAppName}
          AppVersion={#MyAppVersion}
          AppPublisher={#MyAppPublisher}
          DefaultDirName={autopf}\Sakrylle Chat
          DefaultGroupName={#MyAppName}
          OutputDir=.
          OutputBaseFilename=Sakrylle_Chat_windows_{#MyAppVersion}_setup
          Compression=lzma2
          SolidCompression=yes
          WizardStyle=modern
          ArchitecturesInstallIn64BitMode=x64
          ArchitecturesAllowed=x64
          UninstallDisplayIcon={app}\{#MyAppExeName}
          $languagesBlock
          [Tasks]
          Name: "desktopicon"; Description: "创建桌面快捷方式"; GroupDescription: "附加图标:"
          [Files]
          Source: "build\windows\x64\runner\Release\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs
          [Icons]
          Name: "{group}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"
          Name: "{group}\卸载 {#MyAppName}"; Filename: "{uninstallexe}"
          Name: "{autodesktop}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"; Tasks: desktopicon
          [Run]
          Filename: "{app}\{#MyAppExeName}"; Description: "启动 {#MyAppName}"; Flags: nowait postinstall skipifsilent
          "@
          
          # 使用 UTF-8 BOM 以确保中文字符在 Inno Setup 中正确解析
          $issContent | Out-File -FilePath "installer.iss" -Encoding utf8BOM
          
          # 编译安装程序
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "installer.iss"
      - name: 🚀 发布到 Release
        if: ${{ github.event.inputs.publish_release == 'true' && github.event.inputs.release_tag != '' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.release_tag }}
          name: ${{ github.event.inputs.release_tag }}
          prerelease: ${{ github.event.inputs.prerelease }}
          files: |
            Sakrylle_Chat_windows_*.zip
            Sakrylle_Chat_windows_*_setup.exe
      - name: 📤 上传构建产物 (ZIP)
        uses: actions/upload-artifact@v4
        with:
          name: Windows-ZIP
          path: Sakrylle_Chat_windows_*.zip

      - name: 📤 上传构建产物 (Installer)
        uses: actions/upload-artifact@v4
        with:
          name: Windows-Installer
          path: Sakrylle_Chat_windows_*_setup.exe

  # Linux 构建
  linux:
    if: ${{ github.event.inputs.build_linux == 'true' }}
    name: 构建 Linux
    runs-on: ubuntu-22.04
    permissions: write-all

    steps:
      - name: 📦 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔧 安装依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            clang cmake ninja-build pkg-config libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            libunwind-dev \
            libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev \
            gstreamer1.0-plugins-good gstreamer1.0-plugins-bad \
            rpm patchelf libfuse2 file \
            libkeybinder-3.0-dev libayatana-appindicator3-dev
      - name: 🐦 安装 Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: 📝 Inject fallback
        shell: bash
        run: |
          mkdir -p lib/secrets
          cat > lib/secrets/fallback.dart <<'EOF'
          const String siliconflowFallbackKey = '${{ secrets.SILICONFLOW_KEY }}';
          EOF
      - name: 📝 获取版本号
        run: |
          RAW_VERSION=$(grep 'version:' pubspec.yaml | sed 's/version: //' | tr -d ' ')
          VERSION=$RAW_VERSION
          if [ "${{ github.event.inputs.publish_release }}" != "true" ]; then
            SHORT_SHA=$(git rev-parse --short HEAD)
            VERSION="${VERSION}_${SHORT_SHA}"
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "VERSION_NUMBER=$(echo $RAW_VERSION | cut -d'+' -f1)" >> $GITHUB_ENV
      - name: 🏗️ 构建 Linux
        run: flutter build linux --release

      - name: 📦 打包 tar.gz
        run: |
          cd build/linux/x64/release/bundle
          tar -czf ../../../../../${APP_ARTIFACT_NAME}_linux_${VERSION}.tar.gz .
      - name: 📦 创建 AppImage
        run: |
          # 下载 appimagetool
          wget -O appimagetool "https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage"
          chmod +x appimagetool
          
          # 创建 AppDir 结构
          mkdir -p AppDir/usr/bin
          mkdir -p AppDir/usr/lib
          mkdir -p AppDir/usr/share/icons/hicolor/256x256/apps
          cp -r build/linux/x64/release/bundle/* AppDir/usr/bin/
          
          # 创建 AppRun 脚本
          cat > AppDir/AppRun << 'EOF'
          #!/bin/bash
          SELF=$(readlink -f "$0")
          HERE=${SELF%/*}
          export PATH="${HERE}/usr/bin:${PATH}"
          export LD_LIBRARY_PATH="${HERE}/usr/lib:${LD_LIBRARY_PATH}"
          exec "${HERE}/usr/bin/sakrylle_chat" "$@"
          EOF
          chmod +x AppDir/AppRun
          
          # 创建 desktop 文件
          cat > AppDir/${LINUX_APP_ID}.desktop << EOF
          [Desktop Entry]
          Name=Sakrylle Chat
          Exec=sakrylle_chat
          Icon=com.sakrylle.chat
          Type=Application
          Categories=Utility;
          EOF
          
          ICON_SOURCE="assets/sakrylle_icon.png"
          if [ ! -f "$ICON_SOURCE" ]; then
            ICON_SOURCE="linux/kelivo.png"
          fi
          if [ ! -f "$ICON_SOURCE" ]; then
            echo "❌ Icon source not found"
            exit 1
          fi
          install -Dm644 "$ICON_SOURCE" AppDir/${LINUX_APP_ID}.png
          install -Dm644 "$ICON_SOURCE" "AppDir/usr/share/icons/hicolor/256x256/apps/${LINUX_APP_ID}.png"
          
          # 构建 AppImage
          ARCH=x86_64 ./appimagetool AppDir ${APP_ARTIFACT_NAME}_linux_${VERSION}.AppImage
      - name: 📦 创建 DEB 包
        run: |
          # 创建 DEB 目录结构
          mkdir -p deb/DEBIAN
          mkdir -p deb/opt/sakrylle-chat
          mkdir -p deb/usr/share/applications
          mkdir -p deb/usr/share/icons/hicolor/256x256/apps
          
          # 复制文件
          cp -r build/linux/x64/release/bundle/* deb/opt/sakrylle-chat/
          
          # 创建 /usr/bin 软链接
          mkdir -p deb/usr/bin
          ln -s /opt/sakrylle-chat/sakrylle_chat deb/usr/bin/sakrylle-chat
          
          # 创建 control 文件
          cat > deb/DEBIAN/control << EOF
          Package: sakrylle-chat
          Version: ${VERSION_NUMBER}
          Section: utils
          Priority: optional
          Architecture: amd64
          Maintainer: Sakrylle <support@sakrylle.com>
          Depends: libgtk-3-0, libstdc++6, libglu1-mesa, libgstreamer1.0-0, libgstreamer-plugins-base1.0-0, libglib2.0-0, libc6, libwebkit2gtk-4.1-0
          Description: Sakrylle Chat
           AI gateway chat client by Sakrylle API
          EOF
          
          # 创建 desktop 文件
          cat > deb/usr/share/applications/${LINUX_APP_ID}.desktop << EOF
          [Desktop Entry]
          Name=Sakrylle Chat
          Exec=/opt/sakrylle-chat/sakrylle_chat
          Icon=com.sakrylle.chat
          Type=Application
          Categories=Utility;
          EOF

          ICON_SOURCE="assets/sakrylle_icon.png"
          if [ ! -f "$ICON_SOURCE" ]; then
            ICON_SOURCE="linux/kelivo.png"
          fi
          if [ ! -f "$ICON_SOURCE" ]; then
            echo "❌ Icon source not found"
            exit 1
          fi
          install -Dm644 "$ICON_SOURCE" deb/usr/share/icons/hicolor/256x256/apps/${LINUX_APP_ID}.png
          
          # 创建启动脚本
          cat > deb/opt/sakrylle-chat/sakrylle-chat.sh << 'EOF'
          #!/bin/bash
          cd "$(dirname "$0")"
          ./sakrylle_chat "$@"
          EOF
          chmod +x deb/opt/sakrylle-chat/sakrylle-chat.sh
          
          # 构建 DEB 包
          dpkg-deb --build deb ${APP_ARTIFACT_NAME}_linux_${VERSION}_amd64.deb
      - name: 📦 创建 RPM 包
        run: |
          # 创建 RPM 构建目录
          mkdir -p ~/rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
          
          # 创建源码目录
          mkdir -p sakrylle-chat-${VERSION_NUMBER}
          cp -r build/linux/x64/release/bundle/* sakrylle-chat-${VERSION_NUMBER}/
          ICON_SOURCE="assets/sakrylle_icon.png"
          if [ ! -f "$ICON_SOURCE" ]; then
            ICON_SOURCE="linux/kelivo.png"
          fi
          if [ ! -f "$ICON_SOURCE" ]; then
            echo "❌ Icon source not found"
            exit 1
          fi
          cp "$ICON_SOURCE" sakrylle-chat-${VERSION_NUMBER}/${LINUX_APP_ID}.png
          tar -czf ~/rpmbuild/SOURCES/sakrylle-chat-${VERSION_NUMBER}.tar.gz sakrylle-chat-${VERSION_NUMBER}
          
          # 创建 spec 文件
          cat > ~/rpmbuild/SPECS/sakrylle-chat.spec << EOF
          Name:           sakrylle-chat
          Version:        ${VERSION_NUMBER}
          Release:        1%{?dist}
          Summary:        Sakrylle Chat
          License:        MIT
          Source0:        %{name}-%{version}.tar.gz
          BuildArch:      x86_64
          Requires:       gtk3, libstdc++ >= 6, mesa-libGLU, gstreamer1, gstreamer1-plugins-base, glib2, glibc, webkit2gtk4.1
          
          %description
          AI gateway chat client by Sakrylle API
          
          %prep
          %setup -q
          
          %install
          mkdir -p %{buildroot}/opt/sakrylle-chat
          cp -r * %{buildroot}/opt/sakrylle-chat/
          
          mkdir -p %{buildroot}/usr/bin
          ln -s /opt/sakrylle-chat/sakrylle_chat %{buildroot}/usr/bin/sakrylle-chat
          
          mkdir -p %{buildroot}/usr/share/applications
          cat > %{buildroot}/usr/share/applications/com.sakrylle.chat.desktop << 'DESKTOPEOF'
          [Desktop Entry]
          Name=Sakrylle Chat
          Exec=/opt/sakrylle-chat/sakrylle_chat
          Icon=com.sakrylle.chat
          Type=Application
          Categories=Utility;
          DESKTOPEOF

          mkdir -p %{buildroot}/usr/share/icons/hicolor/256x256/apps
          cp com.sakrylle.chat.png %{buildroot}/usr/share/icons/hicolor/256x256/apps/com.sakrylle.chat.png
          
          %files
          /opt/sakrylle-chat/*
          /usr/bin/sakrylle-chat
          /usr/share/applications/com.sakrylle.chat.desktop
          /usr/share/icons/hicolor/256x256/apps/com.sakrylle.chat.png
          
          %changelog
          * $(date "+%a %b %d %Y") Sakrylle <support@sakrylle.com> - ${VERSION_NUMBER}-1
          - Initial RPM release
          EOF
          
          # 构建 RPM
          rpmbuild -bb ~/rpmbuild/SPECS/sakrylle-chat.spec
          cp ~/rpmbuild/RPMS/x86_64/sakrylle-chat-${VERSION_NUMBER}-1.*.rpm ${APP_ARTIFACT_NAME}_linux_${VERSION}.rpm
      - name: 🔍 RPM 元数据（调试）
        shell: bash
        run: |
          set -euxo pipefail
          RPM_FILE="${APP_ARTIFACT_NAME}_linux_${VERSION}.rpm"
          echo "RPM file: ${RPM_FILE}"
          cp -f ~/rpmbuild/SPECS/sakrylle-chat.spec rpm-spec.spec
          rpm -qp --info "${RPM_FILE}" | tee rpm-info.txt
          rpm -qp --requires "${RPM_FILE}" | tee rpm-requires.txt
          rpm -qp --provides "${RPM_FILE}" | tee rpm-provides.txt
      - name: 📤 上传 RPM 元数据（调试）
        uses: actions/upload-artifact@v4
        with:
          name: Linux-RPM-Metadata
          path: |
            rpm-spec.spec
            rpm-info.txt
            rpm-requires.txt
            rpm-provides.txt
      - name: 🧪 Fedora 43 安装测试
        shell: bash
        run: |
          set -euxo pipefail
          RPM_FILE="${APP_ARTIFACT_NAME}_linux_${VERSION}.rpm"
          docker run --rm -v "${PWD}:/work" -w /work fedora:43 bash -lc "dnf -y install ./${RPM_FILE}"
      - name: 🚀 发布到 Release
        if: ${{ github.event.inputs.publish_release == 'true' && github.event.inputs.release_tag != '' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.release_tag }}
          name: ${{ github.event.inputs.release_tag }}
          prerelease: ${{ github.event.inputs.prerelease }}
          files: |
            Sakrylle_Chat_linux_*.tar.gz
            Sakrylle_Chat_linux_*.AppImage
            Sakrylle_Chat_linux_*_amd64.deb
            Sakrylle_Chat_linux_*.rpm
      - name: 📤 上传构建产物 (tar.gz)
        uses: actions/upload-artifact@v4
        with:
          name: Linux-tar.gz
          path: Sakrylle_Chat_linux_*.tar.gz

      - name: 📤 上传构建产物 (AppImage)
        uses: actions/upload-artifact@v4
        with:
          name: Linux-AppImage
          path: Sakrylle_Chat_linux_*.AppImage

      - name: 📤 上传构建产物 (DEB)
        uses: actions/upload-artifact@v4
        with:
          name: Linux-DEB
          path: Sakrylle_Chat_linux_*_amd64.deb

      - name: 📤 上传构建产物 (RPM)
        uses: actions/upload-artifact@v4
        with:
          name: Linux-RPM
          path: Sakrylle_Chat_linux_*.rpm
