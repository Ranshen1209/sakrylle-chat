name: Deprecated Multi-Platform Build

on:
  workflow_dispatch:

jobs:
  deprecated:
    name: Deprecated workflow
    runs-on: ubuntu-latest
    steps:
      - name: Use the stable build workflow
        run: |
          echo "This workflow is deprecated. Use '.github/workflows/build-stable.yml' (Flutter Multi-Platform Build Stable) for current Sakrylle Chat builds."
          exit 1
